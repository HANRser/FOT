# PyTorch/CUDA strongly depends on the GPU driver. For a fresh GPU machine,
# prefer installing the matching PyTorch build from https://pytorch.org first;
# pip will keep that compatible installation when processing this file.
torch>=2.1
torchvision>=0.16

# Frozen Stable-Diffusion VAE used by VAE.py.
diffusers>=0.30,<1.0
transformers>=4.57,<5.0
huggingface_hub>=0.34,<1.0
accelerate>=1.0
safetensors>=0.4

# Perceptual image-fidelity loss used by template_embedding.py.
lpips>=0.1.4

# Test runner.
pytest>=8,<9

# End-to-end reproduction, evaluation and demo.
pillow>=10
numpy>=1.24
opencv-python>=4.8
gradio>=4.0
scikit-image>=0.22
imageio[ffmpeg]>=2.34
